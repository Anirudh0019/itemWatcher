# ItemWatcher Deployment Configuration
# Copy this to /opt/itemwatcher/.env and fill in your values

# ─── Database ────────────────────────────────────────────────────
# Default: ~/.itemwatcher/data.db (relative to the service user's home)
# For deployment, use an absolute path:
ITEMWATCHER_DB_PATH=/opt/itemwatcher/data/data.db

# ─── Scheduler ───────────────────────────────────────────────────
# How often the scheduler checks prices (in hours)
ITEMWATCHER_CHECK_INTERVAL_HOURS=1

# ─── Email Notifications (optional) ─────────────────────────────
# Gmail: use an App Password (https://support.google.com/accounts/answer/185833)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM_EMAIL=your-email@gmail.com
# ALERT_TO_EMAIL=your-email@gmail.com
# SMTP_USE_TLS=true
